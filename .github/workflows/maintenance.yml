name: maintenance

# Run a one-off maintenance task against the production droplet, on demand.
#
# These are the jobs that have to happen on the box because they touch the
# database directly and Postgres is not exposed off the droplet: seeding the
# plans, importing a prospect list, firing the reminder sweep. Before this
# existed they were run by hand over SSH, which meant they were run rarely and
# nobody could see afterwards what had been run.
#
# Every task is idempotent by design, so re-running one is safe:
#   - import-doctors  never duplicates (matched on slug) and never republishes
#     a doctor who asked to be removed
#   - seed-plans      creates the plans only if they are absent
#   - run-reminders   stamps each appointment once
#   - geocode         only fills rows with no point, and never overwrites an
#                     EXACT one with a coarser geocode
#   - reset-pin       undoes one doctor's hand-placed pin back to a geocode,
#                     for when it was dropped at the wrong address
#   - publish-articles  skips any article whose title is already published
#
# Publishing runs inside the container through the controllers, so no production
# admin token is ever minted, pasted into a terminal or carried through a chat
# window. Three variants on purpose: a dry run, one that leaves the batch as
# DRAFTS for a human to read in the console, and one that publishes. The default
# for a first batch should be drafts — approving later is one more click, and
# unpublishing after Google has crawled is not.
#
# Errahma imports from a committed generator; the city-wide list imports from a
# dated snapshot under scripts/data/. Snapshots are tracked on purpose: the
# droplet must not re-scrape third-party directories on every run, and a removal
# request needs a specific dated file with per-row sources to answer from.
#
# Uses the same DO_HOST / DO_USER / DO_SSH_KEY secrets as deploy.yml.

on:
  workflow_dispatch:
    inputs:
      region:
        description: Region snapshot for import-region (marrakech, fes, tanger, agadir, oujda, centre, sud)
        required: false
        type: string
      doctor_id:
        description: Doctor id — required by reset-pin, ignored otherwise
        required: false
        type: string
      task:
        description: What to run on the droplet
        required: true
        type: choice
        options:
          - import-doctors-dry-run
          - import-doctors
          - import-casablanca-dry-run
          - import-casablanca
          - import-rabat-dry-run
          - import-rabat
          - import-region-dry-run
          - import-region
          - seed-plans
          - run-reminders
          - geocode-dry-run
          - geocode
          - reset-pin
          - repair-addresses-dry-run
          - repair-addresses
          - publish-articles-dry-run
          - publish-articles-drafts
          - publish-articles
        default: import-doctors-dry-run

concurrency:
  # Shares the deploy group: never mutate the database while the stack is being
  # rebuilt underneath it.
  group: sehaty-droplet-deploy
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  maintenance:
    runs-on: ubuntu-latest
    env:
      DO_HOST: ${{ secrets.DO_HOST }}
    steps:
      - name: Run ${{ inputs.task }} on the droplet
        if: ${{ env.DO_HOST != '' }}
        uses: appleboy/ssh-action@v1.0.3
        env:
          TASK: ${{ inputs.task }}
          DOCTOR_ID: ${{ inputs.doctor_id }}
          REGION: ${{ inputs.region }}
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USER }}
          key: ${{ secrets.DO_SSH_KEY }}
          envs: TASK,DOCTOR_ID,REGION
          script_stop: true
          command_timeout: 20m
          # appleboy runs this under the droplet's login shell; keep every line
          # a single standalone command (no multi-line if/for/case constructs —
          # the shell fails to parse them and aborts the whole script).
          script: |
            set -eu
            cd /opt/sehaty/sehaty-api/deploy
            test "$TASK" != "seed-plans" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python -c "from sehaty.core.controllers.billing import BillingController; print('plans created', len(BillingController.seed_plans()))"
            test "$TASK" != "run-reminders" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python -c "from sehaty.core.controllers.appointments import AppointmentController; print('reminded', AppointmentController.run_reminders(within_hours=24))"
            test "$TASK" != "import-doctors-dry-run" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api sh -c "python scripts/build_errahma_csv.py > /tmp/doctors.csv && python scripts/import_doctors.py /tmp/doctors.csv --dry-run"
            test "$TASK" != "import-region-dry-run" || test -n "$REGION"
            test "$TASK" != "import-region-dry-run" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/import_doctors.py "scripts/data/$REGION-2026-07-29.csv" --dry-run
            test "$TASK" != "import-region" || test -n "$REGION"
            test "$TASK" != "import-region" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/import_doctors.py "scripts/data/$REGION-2026-07-29.csv"
            test "$TASK" != "import-rabat-dry-run" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/import_doctors.py scripts/data/rabat-2026-07-29.csv --dry-run
            test "$TASK" != "import-rabat" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/import_doctors.py scripts/data/rabat-2026-07-29.csv
            test "$TASK" != "geocode-dry-run" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/geocode_doctors.py --dry-run
            test "$TASK" != "geocode" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/geocode_doctors.py
            test "$TASK" != "import-casablanca-dry-run" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/import_doctors.py scripts/data/casablanca-2026-07-28.csv --dry-run
            test "$TASK" != "import-casablanca" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/import_doctors.py scripts/data/casablanca-2026-07-28.csv
            test "$TASK" != "repair-addresses-dry-run" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/repair_addresses.py --dry-run
            test "$TASK" != "repair-addresses" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/repair_addresses.py
            test "$TASK" != "reset-pin" || test -n "$DOCTOR_ID"
            test "$TASK" != "reset-pin" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/geocode_doctors.py --doctor "$DOCTOR_ID" --force
            test "$TASK" != "publish-articles-dry-run" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/publish_articles.py --file scripts/data/articles/batch-2026-07.jsonl --direct --dry-run
            test "$TASK" != "publish-articles-drafts" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/publish_articles.py --file scripts/data/articles/batch-2026-07.jsonl --direct
            test "$TASK" != "publish-articles" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api python scripts/publish_articles.py --file scripts/data/articles/batch-2026-07.jsonl --direct --publish
            test "$TASK" != "import-doctors" || docker compose -f docker-compose.prod.yml --env-file .env run --rm api sh -c "python scripts/build_errahma_csv.py > /tmp/doctors.csv && python scripts/import_doctors.py /tmp/doctors.csv"
